import React from 'react';
import { Container, Typography } from '@mui/material';

const ReportGenerator = ({ report }) => (
  report ? (
    <>
      {/* Render report details */}
    </>
  ) : null;
);

export default ReportGenerator;
